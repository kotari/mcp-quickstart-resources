[project]
name = "mcp-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.40.0",
    "mcp>=1.1.1",
    "ollama>=0.4.5",
    "python-dotenv>=1.0.1",
    "rich>=13.9.4",
]

[tool.uv]
native-tls = true
